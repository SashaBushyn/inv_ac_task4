server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userDB
    username: postgres
    password: root
  jpa:
    database: POSTGRESQL
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update


